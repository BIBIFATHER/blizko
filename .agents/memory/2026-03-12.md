# 2026-03-12 — Дневник разработки

## Что сделали

### 0. Юридическое
- Добавлены коды ОКВЭД для Blizko ✅ (уточнить какие именно у Антона при следующей сессии)
 — Human-First Support Engine ✅
Полностью реализовали AI-поддержку с нуля:
- `api/ai-support.ts` — Vercel Function с Gemini, sentiment analysis, Telegram эскалацией
- `services/supportEngine.ts` — клиентский сервис для тикетов
- `components/SupportChat.tsx` — переписан: AI typing indicator, эскалационный баннер
- DB миграции: `support_tickets` + `support_messages`

### 2. Исправили цепочку багов деплоя
- Яндекс.Метрика `<noscript>` в `<head>` → parse5 ошибка → перенесли в `<body>`
- `@supabase/supabase-js` ESM/CJS конфликт в Vercel Functions → заменили на fetch REST API
- Все API роуты падали из-за `from './_cors'` без `.js` (ESM resolution) — исправили в `ai.ts` и `ai-support.ts`
- `GEMINI_API_KEY` не был добавлен в Vercel → добавили валидный ключ

### 3. Итог
AI Concierge работает на blizko.app:
- Отвечает по-человечески с sentiment tracking
- При негативном настроении → Telegram уведомление Антону
- Gemini 1.5-flash с fallback на 2.0-flash и 1.5-pro

## Важные уроки
- Vercel Functions (ESM) требуют `.js` расширение во всех локальных импортах
- `@supabase/supabase-js` не работает в Vercel Node Functions — использовать fetch REST вместо SDK
- `GEMINI_API_KEY` (серверная) ≠ `VITE_GEMINI_API_KEY` (клиентская)
