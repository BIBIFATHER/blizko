# 2026-03-13 — Дневник разработки

## Security Hardening Phase 2

### TMA Client Integration
- `aiGateway.ts` — переписан на `apiFetch()` вместо raw `fetch` (авто-инъекция TMA headers)
- `chat.ts` — добавлены TMA headers в alertContactDetected 
- `apiFetch.ts` — исправлен баг import path (`../core/auth/` → `../auth/`)

### TMA Server Validation (soft mode)
- `ai-support.ts` — добавлена мягкая TMA валидация: логирует, не блокирует
- Это безопасно для non-TMA пользователей

### Audit Logging
- Добавлены события: `tma_validated`, `ai_concierge_request`, `ai_concierge_escalated`
- SQL migration `20260313_security_audit_log.sql` — НЕ применена (Supabase MCP не авторизован)

### Оставшиеся ручные шаги
- [ ] Применить SQL migration в Supabase SQL Editor
- [ ] Настроить CORS_ALLOW_ORIGINS в Vercel
- [ ] Тест TMA в реальном Telegram Mini App

## Domain Migration (начато, не завершено)
- Зарегистрировались на Cloudflare, добавили домен blizko.app (Free plan)
- Дошли до страницы DNS records — но браузерная автоматизация не работает (капча Cloudflare)
- Код готов: CSP обновлён, CORS обновлён, Bento cards fixнуты
- **Осталось:** 3 DNS записи + SSL + nameservers в Timeweb + Vercel env
- Антон попросил напомнить завтра → записано в HEARTBEAT.md

## Важные уроки
- `apiFetch.ts` имел broken import path с момента создания — обнаружили только при first build after refactor
- CSP headers уже были в vercel.json — не нужно дублировать работу
- Cloudflare блокирует browser automation капчей — нельзя автоматизировать DNS setup
- **Правило:** всегда писать мнение + рекомендацию модели ПЕРЕД задачей (записано в MEMORY.md)
