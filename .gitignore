# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.playwright/
playwright-report/
test-results/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.claude/
.cleanup_quarantine/
archive/.quarantine/

# Backups
backups/*.tar.gz
archive/backups/*.tar.gz

# Env (local)
.env.local
.env.*.local
.openclaw/
.clawhub/
figma-mcp/node_modules/

# Local secrets / backups
.env.local
backups/secrets/
archive/backups/secrets/
*.tgz
*.zip
.env
.env*.local
awesome-llm-apps
claude-code-templates

# Supabase CLI local state
supabase/.temp/
supabase/.branches/

# Local tool / test artifacts (never product code)
.playwright-mcp/
.vercel/
blizko-check.png

# Local encrypted recruiting registry and plaintext migration exports
.local/recruiting/
nanny-warm-pool-export*.json
nanny-warm-pool-export*.csv

# User document (not product code — never tracked, never deleted by tooling)
/Публичная оферта Blizko для клиентов.docx
