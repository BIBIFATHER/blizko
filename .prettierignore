# Build output / deps
dist
node_modules
package-lock.json
*.n8n

# Agent / tooling workspaces (not product code)
.claude/
.agents/
.agent/
.venv/
.context/
AGENT_PROFILES/
agents/

# Memory & docs (never auto-format)
memory/
docs/
archive/
output/
ops/
# Root-level hand-maintained markdown docs (specs, runbooks, strategy notes).
# These are prose, not source — keep prettier away so format:check can gate code
# without churning docs (see BLI-95).
/*.md

# Generated / vendored / platform artifacts
android/
ios/
public/
cloudflare/
stitch-mcp-server/
