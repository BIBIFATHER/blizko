# 2026-03-26

## Session Log

- Reviewed the current workspace as an operating system, not only as a codebase.
- Confirmed that memory and project documentation are strong, but integrations and AI routing are still partial.
- Upgraded `TOOLS.md` from a generic template into a practical local setup document.
- Added `.context/AI_STACK.md` to define how research, implementation, and deep reasoning should be routed.
- Added `.context/MCP_STACK.md` to document which integrations matter and in what order to connect them.
- Added `.context/BOOTSTRAP_CHECKLIST.md` to show current readiness and next setup steps.
- Turned `HEARTBEAT.md` into a real periodic checklist instead of an empty placeholder.
- Audited `~/.codex/config.toml` and found `figma`, `notion`, and `linear` already configured.
- Added `github` and `supabase` MCP entries to the global Codex config.

## Key Takeaways

- The biggest missing multiplier is MCP connectivity, not more docs.
- The current workspace already has enough strategic material; the next gains come from execution plumbing.
- The right order is: authenticate GitHub / Linear / Supabase first, then add Sentry and research.

## Next Actions

- Connect core MCPs.
- Finish interactive authentication for the new remote MCP entries.
- Add real machine-specific values into `TOOLS.md`.
- Decide whether long-term `MEMORY.md` should be restored in this workspace.

## MCP Diagnosis Update

- `codex mcp list` confirms all five servers are registered in local config.
- `figma` is healthy and visible from the current Codex session.
- `notion` fails with `invalid_grant`, which means saved OAuth refresh state is stale.
- `supabase` returns `Auth required`.
- `github`, `linear`, and `supabase` are marked `Auth: Unsupported` by local `codex` CLI `0.98.0`.
- `linear` additionally times out during handshake from the current client.
- Local environment now has `fzf`, `fd`, and `direnv` installed; `bun` remains absent.

## Upgrade And Recheck

- Upgraded global `@openai/codex` from `0.98.0` to a new CLI build reporting `codex-cli 0.117.0-alpha.12`.
- After upgrade, `codex mcp list` improved:
  - `figma`: `OAuth`
  - `notion`: `OAuth`
  - `linear`: `Not logged in`
  - `github`: still `Unsupported`
  - `supabase`: still `Unsupported`
- Started OAuth login flows for `notion` and `linear`; both now have valid local callback URLs waiting for browser authorization.
- No existing GitHub or Supabase token env vars were found in the shell environment.

## Final MCP State For This Pass

- `notion` OAuth login completed successfully.
- `linear` browser auth did not persist credentials; current status remains `Not logged in`.
- Reconfigured `github` MCP to use `bearer_token_env_var = "GITHUB_TOKEN"`.
- Reconfigured `supabase` MCP to use `bearer_token_env_var = "SUPABASE_ACCESS_TOKEN"`.
- Current `codex mcp list` status:
  - `figma`: `OAuth`
  - `notion`: `OAuth`
  - `linear`: `Not logged in`
  - `github`: `Bearer token`
  - `supabase`: `Bearer token`
- Shell environment still does not contain `GITHUB_TOKEN` or `SUPABASE_ACCESS_TOKEN`.

## Linear Recheck

- Rechecked after user-reported Linear login.
- `codex mcp list` still reports `linear` as `Not logged in`.
- `list_mcp_resources(server="linear")` still fails with handshake timeout.
- Treat Linear auth as unresolved in the current Codex environment.

## Machine Setup And Delivery Audit

- `TOOLS.md` was updated with real machine facts: available browsers, host machine name, hardware profile, no existing SSH config, and local shell shortcuts.
- `MEMORY.md` was created because this repo is being treated as a main-session workspace.
- `npm test` passed: 7 test files, 17 tests green.
- `npm run build` passed successfully.
- `npm run lint` was reduced from 37 errors / 97 warnings to 0 errors / 101 warnings by cleaning up ESLint false positives and React render/ref violations.
- There are no `.github/workflows/*` files in the repo, so CI is currently local/Vercel-centric rather than GitHub Actions-based.
- Sentry is only partially wired: build-time plugin and auth vars exist, but current `.env.local` does not include `VITE_SENTRY_DSN`, so browser-side runtime error capture is not active in this environment.

## CI Follow-Up

- Added `.github/workflows/ci.yml`.
- The repo now has a minimal GitHub Actions pipeline: `npm ci` → `npm run lint` → `npm test` → `npm run build`.
- CI still needs first remote execution in GitHub to validate cache behavior and environment assumptions.

## Antigravity Tuning

- Switched focus away from OpenClaw entirely; user wants only Antigravity configured.
- Confirmed Antigravity binary exists at `~/.antigravity/antigravity/bin/antigravity` and reports version `1.107.0`.
- Real Antigravity user state lives in `~/Library/Application Support/Antigravity`, not in `~/.antigravity/User`.
- Current user settings were almost empty; added stabilizing user-level settings:
  - `"telemetry.telemetryLevel": "off"`
  - `"update.mode": "manual"`
  - `"extensions.autoCheckUpdates": false`
  - disabled Java-related telemetry/error-log collection
- Updated `~/.antigravity/argv.json` to disable crash reporting.
- Moved recreatable Antigravity caches to `~/.Trash/antigravity-cleanup-2026-03-26`:
  - `Cache`
  - `CachedData`
  - `Code Cache`
  - `GPUCache`
- Root cause still not fully fixed because the machine cannot resolve `localhost`:
  - `python socket.getaddrinfo('localhost', ...)` fails
  - `/etc/hosts` is effectively empty
  - this likely breaks Antigravity local proxy / auth / feature-flag flows
- Could not repair `/etc/hosts` automatically because `sudo` requires a password in this environment.
- Antigravity logs also show recurring network failures to Google/OpenVSX endpoints and built-in analytics/update services; many of those should quiet down after the user-level telemetry/update changes plus a restart.
- One recurring extension-host problem comes from `kilocode.kilo-code`, which was logging `ENOTFOUND localhost` and unresponsive extension host events. Did not disable extensions yet because the main blocker is still the broken system `localhost` mapping.

## Codex Mirror Protocol

- Added `.context/CODEX_OPERATING_PROTOCOL.md` to mirror the same operating discipline configured for Antigravity.
- Updated `AGENTS.md` so future Codex sessions explicitly load the Codex operating protocol on startup.
- Synced `.context/AI_STACK.md` so Codex and Antigravity share one routing philosophy for model choice, memory discipline, research rigor, and agent/skill orchestration.

## Codex Operating Hardening

- Added `.context/CODEX_REVIEW_PROTOCOL.md` so review requests default to findings-first, severity-ordered output.
- Added `.context/CODEX_RELEASE_PROTOCOL.md` so release-readiness claims require explicit lint / test / build / env / risk / rollback reporting.
- Added `.context/CODEX_DECISION_LOGGING.md` so operating changes and lessons are written to files immediately instead of being left in chat.
- Hardened `.context/CODEX_OPERATING_PROTOCOL.md` and `.context/AI_STACK.md` with a zero-exception rule for freshness-sensitive research: browse first, synthesize second.
