# 2026-03-28

## Session Log

- Added a durable rule for proactive plugin prompting.
- Updated `.context/WORKING_CONTRACT.md` so future Codex passes suggest the exact plugin only when it removes real friction, instead of listing integrations abstractly.
- Defined default trigger cases for `Linear`, `Notion`, `Figma`, `GitHub`, `Sentry`, `Vercel`, `Google Calendar`, and `Gmail`.
- Added user-facing guidance to keep plugin prompts short: one tool, one reason, one immediate next step.

## Why This Matters

- The workspace now has an explicit rule for when Codex should nudge toward the right external system of record.
- This should reduce wasted motion and make proactive hints feel timely rather than noisy.
