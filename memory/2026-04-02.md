# 2026-04-02

## Session Notes

- User asked what is needed for Codex to operate at maximum leverage in this workspace.
- Established preferred collaboration contract:
  - concise task framing
  - one main goal per session
  - execution over discussion
  - explicit access/permission requests when blocked
- Updated `USER.md` and `MEMORY.md` to persist these preferences.

## Practical Access Needs

- Live access to `Linear` for prioritization and active work context.
- Live access to `Supabase` for schema, RLS, and data validation.
- Live access to `GitHub` for PR, CI, and repository state.
- Live access to `Vercel` and `Sentry` for deploy/runtime verification.
- Live access to `Figma` and `Notion` for design/spec source of truth.
- Stable shell-visible env vars / `.env` access for integration-dependent work.

## Access Audit

- `Linear`: authenticated and working.
- `Notion`: authenticated and readable.
- `Figma`: authenticated, but available plans currently show `View` seats only.
- `GitHub`: CLI authenticated; scopes include `repo` and `workflow`.
- `Supabase`: CLI authenticated; projects list works.
- `Vercel`: CLI authenticated, but local repo link is missing.
- Shell env: integration tokens are mostly not exported in shell; many secrets exist only in `.env.local`.
- Sentry: build-time vars exist in `.env.local`; runtime `VITE_SENTRY_DSN` is still absent there.

## Env Workflow

- Added `scripts/with-env.sh` for one-off commands that need `.env.local`.
- Added `scripts/source-env.sh` for interactive shell loading.
- Added `npm run env:doctor` and `npm run env:doctor:local` for non-secret env validation.
- Chosen approach avoids pushing project secrets into global shell dotfiles.

## Vercel / Sentry Follow-up

- `vercel pull --yes --environment=preview` succeeded and created `.vercel/project.json`.
- Repo is now locally linked enough for CLI env/project operations.
- `VITE_SENTRY_DSN` is missing both in local `.env.local` and in Vercel preview env.
