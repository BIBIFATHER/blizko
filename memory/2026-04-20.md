# 2026-04-20

## Ops

- Installed Claude Code native build `2.1.114` via `https://claude.ai/install.sh`.
- Initial install failed because the system volume had only ~146 MiB free; cleared npm cache (`~/.npm`, ~7.2 GiB before cleanup), leaving ~4.8 GiB free before retry.
- Added `export PATH="$HOME/.local/bin:$PATH"` to `/Users/anton/.zshrc` so interactive zsh sessions can run `claude`.
- Verification passed with `zsh -ic 'command -v claude && claude --version'`.

